{
  "version": 2,
  "devCommand": "uvicorn blog.api.main:app --host 0.0.0.0 --port 3000",
  "builds": [
    {
      "src": "Dockerfile.prod",
      "use": "@vercel/docker"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "blog/api/main.py"
    }
  ]
}